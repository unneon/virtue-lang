n = 1
while n <= 500
    divisor = 1
    sum = 0
    while divisor < n
        if n % divisor == 0
            sum = sum + divisor
        divisor = divisor + 1
    if sum == n
        print "{n}"
    n = n + 1

# ignore-llvm
# 6
# 28
# 496
